info:
  name: List Games
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}/games?page=1&per_page=10"
  auth:
    type: bearer
    bearer: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
