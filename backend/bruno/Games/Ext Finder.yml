info:
  name: Ext Finder
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}/games/ext-finder?q={{QUERY}}"
  auth:
    type: bearer
    bearer: "{{TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
